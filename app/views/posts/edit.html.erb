<%= simple_form_for @post, :url => { :action => "update" }  do |f| %>
  <%= f.input :title, :placeholder => 'Title', :class => "input-text" %>
  <%= f.input :body %>
  <%= f.button :submit, :class=> "nice"%>
<% end %>